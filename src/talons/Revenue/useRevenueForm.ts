import { Form } from "antd";

const useRevenueForm = () => {
    const [form] = Form.useForm();

    const onSubmit = () => {};

    const onChange = () => {};

    const handleCancel = () => {};

    return {
        form,
        onSubmit,
        onChange,
        handleCancel,
    };
};
export { useRevenueForm };
